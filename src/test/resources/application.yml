spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/flow_engine
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: test-jwt-secret-key-minimum-32-bytes-long

